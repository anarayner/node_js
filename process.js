//allows you to set specific commands
console.log(process.argv);

if (Math.random > 0.1) {
  while (true) {}
} else {
  console.log('Program execution completed');

  // we can urgently complete the process
  process.exit();
}
